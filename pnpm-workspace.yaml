packages:
  - 'packages/*'
  - 'apps/*'

# Shared dependency versions, referenced with `catalog:` in each package's
# package.json. Grow this as more deps become shared across packages
# (Phase 2 adds Sanity, Phase 3 the design system + Storybook).
catalog:
  eslint: ^9.39.5
  typescript: ^5.9.3
  vitest: ^2.1.9
